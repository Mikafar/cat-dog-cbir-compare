[server]
headless = true
port = 8501
enableXsrfProtection = true

[browser]
gatherUsageStats = false

[theme]
base = "dark"
primaryColor = "#FF6B35"

# Streamlit Community Cloud: if the app exceeds the free-tier memory while
# loading the 3 encoders, enable "High performance" mode in App Settings ->
# Resources, or set this app to run on the default tier (best effort).